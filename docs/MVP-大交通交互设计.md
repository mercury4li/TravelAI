# TravelAI MVP 大交通交互设计稿

> 版本：v0.1  
> 日期：2026-05-10  
> 目标：将 MVP 聚焦为「对话式大交通订票助手」，帮助用户从模糊出行想法快速收敛到可比较、可核验、可跳转预订的交通方案。

---

## 1. MVP 产品定义

### 1.1 一句话定位

用对话帮用户快速找到并核验最适合的机票方案，而不是让用户自己刷航班列表。

### 1.2 核心用户价值

- 把自然语言需求自动转成结构化查询条件。
- 主动追问最关键的缺失信息，减少一次性表单压力。
- 将搜索结果整理成「最省钱 / 最省时间 / 综合推荐」三类决策卡。
- 对航班、价格、余票等事实信息展示数据来源、查询时间和跳转核验入口。
- 支持用户继续用自然语言调整方案，例如「不要红眼」「只看直飞」「预算提高一点」。

### 1.3 MVP 范围

| 模块 | MVP 是否包含 | 说明 |
| --- | --- | --- |
| 对话式需求收集 | 是 | 首屏主体验 |
| 机票查询 | 是 | 先做查询与跳转，不做站内支付 |
| 多方案比较 | 是 | 最省钱、最省时间、综合推荐 |
| 数据来源与核验 | 是 | 必须展示真实信息源链接、查询时间、价格有效期 |
| 本轮偏好理解 | 是 | 只保留当前对话内偏好，例如不要红眼、优先直飞 |
| 火车票 | 不包含 | 机票链路跑通后再扩展 |
| 酒店与完整行程 | 不包含 | MVP 暂时只做订票 |
| 地图与景点排程 | 不包含 | 订票链路跑通后再评估 |
| 保存 / 分享 / 价格提醒 | 不包含 | 首轮不引入账号、权限和定时任务 |

---

## 2. 交互流程图

```mermaid
flowchart TD
    A["用户进入 TravelAI"] --> B["用自然语言描述出行想法"]
    B --> C["AI 解析出行意图"]
    C --> D{关键信息是否足够?}
    D -- 否 --> E["AI 只追问一个最关键问题"]
    E --> B
    D -- 是 --> F["生成结构化查询条件"]
    F --> G["调用交通数据源查询"]
    G --> H{是否拿到可核验结果?}
    H -- 否 --> I["明确提示暂无可靠数据 / 未核验"]
    I --> J["建议修改日期、城市或跳转官方渠道核实"]
    H -- 是 --> K["归类候选方案"]
    K --> L["展示三类推荐卡：最省钱 / 最省时间 / 综合推荐"]
    L --> M{用户是否满意?}
    M -- 否 --> N["用户用自然语言调整筛选条件"]
    N --> F
    M -- 是 --> O["查看详情与预订跳转"]
    O --> P["跳转真实票源核验 / 预订"]
    P --> Q["用户返回继续调整条件"]
```

---

## 3. 对话状态机

```mermaid
stateDiagram-v2
    [*] --> Idle: 打开产品
    Idle --> CollectingIntent: 用户输入旅行想法
    CollectingIntent --> Clarifying: 缺少关键参数
    Clarifying --> CollectingIntent: 用户补充信息
    CollectingIntent --> Searching: 参数足够
    Searching --> NoVerifiedData: 无可靠结果
    NoVerifiedData --> Clarifying: 修改条件
    Searching --> Comparing: 有可核验结果
    Comparing --> Refining: 用户提出偏好修正
    Refining --> Searching: 重新查询
    Comparing --> Selected: 用户选定方案
    Selected --> ExternalBooking: 跳转核验 / 预订
    ExternalBooking --> [*]
```

---

## 4. 用例图

```mermaid
flowchart LR
    U["用户"]:::actor
    S["TravelAI 大交通助手"]:::system
    D["交通数据源 / 飞猪 FlyAI / 官方渠道"]:::external
    U --> UC1["描述出行需求"]
    U --> UC2["补充日期、人数、预算、偏好"]
    U --> UC3["调整筛选条件"]
    U --> UC4["查看方案比较"]
    U --> UC5["跳转核验 / 预订"]

    S --> UC8["解析自然语言为结构化条件"]
    S --> UC9["追问缺失信息"]
    S --> UC10["查询机票"]
    S --> UC11["生成推荐理由"]
    S --> UC12["标注数据来源与查询时间"]

    UC10 --> D
    UC12 --> D

    classDef actor fill:#fff7e6,stroke:#b7791f,color:#1f2937;
    classDef system fill:#e8f3ff,stroke:#2563eb,color:#1f2937;
    classDef external fill:#f3f4f6,stroke:#6b7280,color:#1f2937;
```

---

## 5. 信息架构

```mermaid
flowchart TD
    A["MVP 首页 / 当前任务"] --> B["对话区"]
    A --> C["需求面板"]
    A --> D["方案比较区"]
    A --> E["详情与核验区"]

    B --> B1["用户输入"]
    B --> B2["AI 追问"]
    B --> B3["AI 推荐解释"]
    B --> B4["快捷回复"]

    C --> C1["出发地 / 目的地"]
    C --> C2["出发 / 返程日期"]
    C --> C3["人数 / 舱位"]
    C --> C4["预算"]
    C --> C5["直达 / 中转"]
    C --> C6["时间偏好"]

    D --> D1["最省钱"]
    D --> D2["最省时间"]
    D --> D3["综合推荐"]

    E --> E1["日期 + 航班号"]
    E --> E2["退改 / 行李 / 中转说明"]
    E --> E3["真实信息源链接"]
    E --> E4["跳转核验 / 预订"]
```

---

## 6. 核心页面原型说明

### 6.1 首屏布局

- 左侧：对话流，承载自然语言输入、AI 追问、AI 推荐解释。
- 右侧上方：当前需求面板，让用户随时看到 AI 理解了什么。
- 右侧中部：三张推荐卡，降低搜索结果比较成本。
- 右侧底部：数据可信度、查询时间、真实信息源链接、订票后续动作。

### 6.2 关键组件

| 组件 | 目的 | 交互 |
| --- | --- | --- |
| 对话输入框 | 用户自然语言表达需求 | 支持一句话需求、筛选调整、追问 |
| 需求槽位面板 | 防止用户在长对话中迷路 | 可点击编辑单个字段 |
| 快捷回复 | 降低下一步选择成本 | 例如「优先省钱」「只看直飞」「不要红眼」 |
| 推荐方案卡 | 把列表变成决策 | 三类卡片并排比较 |
| 核验信息条 | 建立信任 | 展示真实信息源、查询时间、跳转 |
| 核验 / 预订按钮 | 进入真实票源 | 跳转核验价格、余票、行李额和退改规则 |

---

## 7. 原型文件

低保真 HTML 原型：

[打开原型：mvp-transport-chat.html](/Users/a1234/GolandProjects/TravelAI/docs/prototypes/mvp-transport-chat.html)

技术方案：

[打开技术方案：MVP-订票技术方案.md](/Users/a1234/GolandProjects/TravelAI/docs/MVP-订票技术方案.md)

---

## 8. 评审问题

1. 结果推荐是否固定三类：「最省钱 / 最省时间 / 综合推荐」？
2. 机票数据源优先级是否采用 FlyAI / 飞猪作为第一版验证？
3. 无实时价格时，前端是否统一展示「价格以票源页面为准」？
